# Исправление отображения валюты

## ✅ ПРОБЛЕМА РЕШЕНА

### Что было исправлено:

1. **Поврежденные символы** - заменены корректными эмодзи:
   - `�` → `💰` в статусах заказов

2. **Символ рубля** - заменен на текст "руб" для лучшей совместимости:
   - `₽` → `руб` во всех текстах

3. **Функция форматирования** - обновлена для включения валюты:
   - `format_price(1500)` теперь возвращает `"1500 руб"`

4. **Устранено дублирование** - убрано "руб" из шаблонов, где уже есть в `format_price()`

### Обновленные файлы:

1. **`app/utils/texts.py`**:
   - Исправлены поврежденные символы в `ORDER_STATUSES` и `FAQ_STATUSES`
   - Заменены все `₽` на `руб`
   - Убрано дублирование валюты в шаблонах

2. **`app/utils/helpers.py`**:
   - Функция `format_price()` теперь добавляет " руб" к сумме

### Результат:

Теперь везде в боте суммы отображаются корректно:
- ✅ `1500 руб` (вместо поврежденных символов)
- ✅ Нет дублирования "руб руб"
- ✅ Единообразное отображение валюты по всему боту

### Примеры:

**Корзина:**
```
🛒 Ваша корзина:
• Борщ x2 = 800 руб
• Котлеты x1 = 400 руб
💰 Общая сумма: 1200 руб
```

**Оплата:**
```
💳 Оплата
💰 Сумма к оплате: 1500 руб
```

**Детали заказа:**
```
📋 Заказ #123
💰 Сумма: 1200 руб
```

Все суммы теперь отображаются с корректным обозначением валюты! 🎉
